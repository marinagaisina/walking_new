/**
 * An object that keeps the field's dimention values.
 * @property {int} rowsCount Number of rows in the field.
 * @property {int} colsCount Number of columns in the field.
 */

 const config = {
     rowsCount: 10,  //y
     colsCount: 10,  //x
 };